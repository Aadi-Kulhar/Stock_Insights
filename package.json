{
  "name": "investment-insights",
  "private": true,
  "description": "AI-powered investment analytics dashboard",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.4"
  }
}
